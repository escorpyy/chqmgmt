# Optional when using the guided PostgreSQL setup screen.
# If set, the application starts directly without showing setup.html.
DATABASE_URL=postgresql://postgres:password@localhost:5432/cheque_tracker

# Use a long random value in every environment.
SESSION_SECRET=replace-with-a-long-random-secret

# Optional; defaults to 3000.
PORT=3000